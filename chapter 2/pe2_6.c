//pe2_6.c
#include <stdio.h>

int main(void)
{
    int liczba = 10;
    int x2, kwadrat;
    x2 = 2 * liczba;
    kwadrat = liczba * liczba;
    printf("liczba to %d, podwojona to %d, kwadrat tej liczby to %d", liczba, x2, kwadrat);
    return 0;
}
